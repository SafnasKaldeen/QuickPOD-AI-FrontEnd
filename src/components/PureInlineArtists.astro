---
interface Props {
  z: string[];
}

const { artists } = Astro.props;
---

{
  artists.map((artist, index) => (
    <>
      {artist}
      {index === artists.length - 1 ? " " : ", "}
    </>
  ))
}
