<script>
  async function fetchProfileDetails() {
    const token =
      "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJzYWZuYXN0aGVncmVhdEBnbWFpbC5jb20iLCJleHAiOjE3MTk5MzIwODV9.8c9cvMZPD41XAQWqkcij3b1mKrCLW44kcsjarYqxSqw";
    const url = "http://127.0.0.1:8000/profile";

    try {
      const response = await fetch(url, {
        headers: {
          Accept: "application/json",
          Authorization: `Bearer ${token}`,
        },
      });

      console.log(response);

      if (!response.ok) {
        throw new Error(`HTTP error! Status: ${response.status}`);
      }

      const data = await response.json();

      // Update HTML with fetched profile details
      document.getElementById("full-name").textContent = data.full_name;
      document.getElementById("email").innerHTML =
        `<a href="/cdn-cgi/l/email-protection" class="__cf_email__ text-blue-500">${data.email}</a>`;
      document.getElementById("language").textContent = data.language;
      document.getElementById("gender").textContent = data.gender;
      document.getElementById("country").textContent = data.country;
      document.getElementById("bot-name").textContent = data.bot_name;
    } catch (error) {
      console.error("Error fetching profile details:", error);
    }
  }

  // Fetch profile details when the script is executed
  fetchProfileDetails();
</script>

<div class="card-body p-4">
  <div class="row">
    <div class="col-sm-3">
      <h6 class="mb-0">Full Name</h6>
    </div>
    <div class="col-sm-9 text-gray-500" id="full-name">Loading...</div>
  </div>
  <hr class="my-2" />
  <div class="row">
    <div class="col-sm-3">
      <h6 class="mb-0">Email</h6>
    </div>
    <div class="col-sm-9 text-gray-500" id="email">Loading...</div>
  </div>
  <hr class="my-2" />
  <div class="row">
    <div class="col-sm-3">
      <h6 class="mb-0">Language</h6>
    </div>
    <div class="col-sm-9 text-gray-500" id="language">Loading...</div>
  </div>
  <hr class="my-2" />
  <div class="row">
    <div class="col-sm-3">
      <h6 class="mb-0">Gender</h6>
    </div>
    <div class="col-sm-9 text-gray-500" id="gender">Loading...</div>
  </div>
  <hr class="my-2" />
  <div class="row">
    <div class="col-sm-3">
      <h6 class="mb-0">Country</h6>
    </div>
    <div class="col-sm-9 text-gray-500" id="country">Loading...</div>
  </div>
  <hr class="my-2" />
  <div class="row">
    <div class="col-sm-3">
      <h6 class="mb-0">Bot Name</h6>
    </div>
    <div class="col-sm-9 text-gray-500" id="bot-name">Loading...</div>
  </div>
</div>
